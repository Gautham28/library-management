# Copy this file to .env and fill in local values (optional helper for your shell).
# Spring Boot reads environment variables directly — you do not need dotenv for the app.

SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

DB_URL=jdbc:postgresql://localhost:5432/library_db
DB_USERNAME=postgres
DB_PASSWORD=postgres
